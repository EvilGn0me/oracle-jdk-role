---
# vars file for oracle-jdk-role
jdk:
  jdkroot: '/opt/jdk'
  installdir: '/usr/share/jdk-{{ jdk_conf.version }}'
  download_url: 'https://build.funtoo.org/distfiles/oracle-java/jdk-{{ jdk_conf.version }}_linux-x64_bin.tar.gz'
