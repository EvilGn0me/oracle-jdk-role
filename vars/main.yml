---
# vars file for oracle-jdk-role
jdk:
  jdkroot: '/opt/jdk'
  installdir: '/usr/share/jdk-{{ jdk_conf.version }}'
  download_url: 'https://download.java.net/java/GA/jdk{{ jdk_conf.version }}/{{ jdk_conf.version }}/binaries/openjdk-{{ jdk_conf.version }}_linux-x64_bin.tar.gz'
